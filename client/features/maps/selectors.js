export const getLayer = (state, ix) => state.maps.layers[ix];
export const getLayerCount = (state) => state.maps.layers.length;

export const getLayerCount = (state) => 2;
export const getLayer = (state, ix) => {
  switch(ix) {
    case 0: {
    }
    case 1: {
    }
  }
}
